name: Python application

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions:
  id-token: write
  issues: write
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:

    - name: Github Issue Creator
      uses: thomasvjoseph/github-issues@v0.0.5
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        title: "New issue"
        labels: "bug,help wanted   s "
        body: "This is a new issue"
        

   